/**
 * http://usejsdoc.org/
 */

angular.module("myApp")
	.factory('CaseManager', CaseManager);

CaseManager.$inject = ['ollCases'];

function CaseManager(ollCases) {
	var factory;
	
	
	
	return factory;
}